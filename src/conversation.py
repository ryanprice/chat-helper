# Conversation history is intentionally stateless for command interactions.
# Each /expand or /condense is a fresh call — no stale context.
# This module is a placeholder for future per-user DM history if needed.
